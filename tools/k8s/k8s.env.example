# Copy to tools/k8s/k8s.env. Values must match k8s/build-gpu.pod.yaml. k8s.env is gitignored.

# Optional kube context.
# KUBE_CONTEXT=

NAMESPACE=tai-production
POD_NAME=build-gpu
CONTAINER_NAME=build
REMOTE_ROOT=/workspace

# Optional REMOTE_DIR.
# REMOTE_DIR=
